<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:textColor">@color/normalTextColor</item>

        <item name="headlineTextColor">@color/headlineTextColor</item>
        <item name="boldTextColor">@color/boldTextColor</item>
        <item name="cardBackgroundColor">@color/card_background</item>

        <item name="cardTitleSize">18dp</item>
        <item name="boldTextSize">16dp</item>
    </style>

</resources>
